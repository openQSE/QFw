qiskit==2.2.3
qiskit_aer==0.17.2
qiskit-algorithms
qiskit-optimization
supermarq
pennylane
pennylane-qiskit
qiskit-ibm-runtime
iqm-client==34.0.1
iqm-data-definitions==2.24
iqm-exa-common==27.5.1
iqm-pulse==13.0.1
iqm-station-control-client==12.1.1
qiskit_nature
mpi4py
networkx
matplotlib
numpy
scipy
pyscf
PyYAML
jsonschema>=4
netifaces
psutil
paramiko
rich

# OpenTelemetry: benchmarking and profiling telemetry, see
# docs/benchmarking-design.md. QFw degrades to no-op instrumentation when
# these are absent, so they are a soft dependency rather than a hard one.
opentelemetry-api>=1.44.0
opentelemetry-sdk>=1.44.0
# Supplies the OTLP encoders. The SDK ships no file exporter, so the file
# profile encodes with these and writes OTLP/JSON itself.
opentelemetry-exporter-otlp-proto-common>=1.44.0
