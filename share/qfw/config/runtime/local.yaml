resolver:
  scope-order:
    - local

local-services:
  start-prte: true
  start-dirsvc: true
  start-qpm: true
  dirsvc:
    name: qfw-local-dirsvc
    bind-host: 127.0.0.1
    port: auto
  service-manifest: ${QFW_PREFIX}/share/qfw/config/services/local-services.yaml
